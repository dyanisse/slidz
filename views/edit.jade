extends layout

block content
  h1= title
  p #{deck.name}
  div
   a(href="https://s3.amazonaws.com/slidz_ppt/"+deck.s3_key) #{"Download file"}
  br
  p #{"Slideshare URL:"}
  div
   a(href=deck.slideshare_url) url
  p
  //button(class="btn btn-large btn-primary", type="button", onclick="update_deck();", onsucess="alert('success');") Save